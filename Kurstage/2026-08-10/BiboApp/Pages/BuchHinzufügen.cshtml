@page
@model BuchHinzufügenModel

<form method="post">
    Titel:
    <input type="text" name="titel" />
    Autor:
    <select name="autorId">
        @foreach(var autor in Model.Autoren)
        {
            <option value="@autor.Id">@autor.Name</option>
        }
    </select>
    <input type="submit" value="Buch hinzufügen">
</form>