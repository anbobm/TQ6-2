# Aufgabe 122

Überlege dir, wie das Nutzerinterface aussehen soll, wenn wir die Datenbank dahingehend umbauen, dass der `Autor` im `Buch` nicht einfach ein String ist, sondern eine Referenz auf einen Datensatz in der `Autoren`-Tabelle. Wie soll der Benutzer beim Anlegen von Datensätzen über unsere Webseite diese Beziehung angeben können?